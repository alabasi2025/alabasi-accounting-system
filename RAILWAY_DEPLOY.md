# 🚀 دليل النشر على Railway.app

## نشر تلقائي بنقرة واحدة

يمكنك نشر هذا النظام على Railway.app بشكل تلقائي ومجاني.

---

## ✨ المزايا

- ✅ نشر تلقائي من GitHub
- ✅ رابط دائم HTTPS مجاني
- ✅ دعم كامل لـ PHP + MySQL
- ✅ 500 ساعة مجانية شهرياً
- ✅ تحديثات تلقائية عند كل push

---

## 📋 خطوات النشر (5 دقائق)

### 1️⃣ إنشاء حساب على Railway

1. اذهب إلى: https://railway.app
2. اضغط "Login" أو "Start a New Project"
3. سجل دخول باستخدام:
   - GitHub (موصى به)
   - أو Google
   - أو البريد الإلكتروني

### 2️⃣ إنشاء مشروع جديد

1. من لوحة التحكم، اضغط "New Project"
2. اختر "Deploy from GitHub repo"
3. اختر المستودع: `alabasi2025/alabasi-accounting-system`
4. اضغط "Deploy Now"

### 3️⃣ إضافة قاعدة بيانات MySQL

1. في المشروع، اضغط "+ New"
2. اختر "Database"
3. اختر "Add MySQL"
4. انتظر حتى يتم إنشاء القاعدة

### 4️⃣ ربط المتغيرات

Railway سيقوم تلقائياً بإنشاء متغيرات البيئة للاتصال بقاعدة البيانات:
- `MYSQL_HOST`
- `MYSQL_DATABASE`
- `MYSQL_USER`
- `MYSQL_PASSWORD`
- `MYSQL_PORT`

### 5️⃣ استيراد قاعدة البيانات

**الطريقة A: عبر Railway CLI**
```bash
# تثبيت Railway CLI
npm install -g @railway/cli

# تسجيل الدخول
railway login

# الاتصال بالمشروع
railway link

# استيراد قاعدة البيانات
railway run mysql -h ${{MYSQL_HOST}} -u ${{MYSQL_USER}} -p${{MYSQL_PASSWORD}} ${{MYSQL_DATABASE}} < database.sql
```

**الطريقة B: عبر phpMyAdmin**
1. من لوحة التحكم في Railway، افتح قاعدة البيانات
2. اضغط "Connect"
3. استخدم بيانات الاتصال لفتح phpMyAdmin
4. استورد ملف `database.sql`

### 6️⃣ الحصول على الرابط

1. من لوحة التحكم، اذهب إلى "Settings"
2. في قسم "Domains"، اضغط "Generate Domain"
3. سيتم إنشاء رابط مثل: `your-app.up.railway.app`

---

## ⚙️ الإعدادات المطلوبة

### ملف `railway.json` (موجود بالفعل)

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT -t .",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### ملف `nixpacks.toml` (موجود بالفعل)

```toml
[phases.setup]
nixPkgs = ['php82', 'php82Extensions.mysqli', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql']

[phases.install]
cmds = ['echo "Installing dependencies..."']

[start]
cmd = 'php -S 0.0.0.0:$PORT -t .'
```

---

## 🔄 التحديثات التلقائية

بعد النشر، أي تحديث تقوم به على GitHub سيتم نشره تلقائياً:

```bash
# في جهازك المحلي
git pull origin master
# عمل تعديلات
git add .
git commit -m "تحديث جديد"
git push origin master

# Railway سيقوم بالنشر تلقائياً!
```

---

## 🐛 حل المشاكل

### المشكلة: الموقع لا يعمل
**الحل:**
1. تحقق من السجلات (Logs) في Railway
2. تأكد من أن قاعدة البيانات متصلة
3. تحقق من متغيرات البيئة

### المشكلة: خطأ في قاعدة البيانات
**الحل:**
1. تأكد من استيراد ملف `database.sql`
2. تحقق من بيانات الاتصال
3. تأكد من أن القاعدة تستخدم `utf8mb4_unicode_ci`

### المشكلة: الصفحة بيضاء
**الحل:**
1. افتح السجلات (Logs)
2. تحقق من وجود أخطاء PHP
3. تأكد من أن جميع الملفات موجودة

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- راجع التوثيق: https://docs.railway.app
- افتح Issue على GitHub
- اتصل بالدعم الفني لـ Railway

---

## 💰 التكلفة

**الخطة المجانية:**
- 500 ساعة تشغيل شهرياً
- 100 GB نقل بيانات
- كافية لمعظم الاستخدامات

**إذا احتجت أكثر:**
- الخطة المدفوعة تبدأ من $5/شهر
- دفع حسب الاستخدام

---

## ✅ الخلاصة

Railway.app يوفر:
- ✅ نشر سريع وسهل
- ✅ تحديثات تلقائية
- ✅ رابط دائم HTTPS
- ✅ مجاني للبداية
- ✅ احترافي وموثوق

**مبروك! نظامك الآن على الإنترنت! 🎉**
