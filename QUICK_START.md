# 🚀 دليل البدء السريع

## للمطورين والمستخدمين الجدد

هذا الدليل يساعدك على تشغيل النظام بسرعة في أقل من 10 دقائق!

---

## ⚡ الطريقة الأسرع: Docker

إذا كان لديك Docker مثبت:

```bash
# 1. استنساخ المشروع
git clone https://github.com/alabasi2025/alabasi-accounting-system.git
cd alabasi-accounting-system

# 2. تشغيل النظام
docker-compose up -d

# 3. افتح المتصفح
http://localhost:8080
```

**بيانات الدخول:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

✅ **انتهى!** النظام يعمل الآن.

---

## 💻 الطريقة الثانية: XAMPP/WAMP

### الخطوة 1: تحميل المشروع
```bash
git clone https://github.com/alabasi2025/alabasi-accounting-system.git
```

أو حمّل ملف ZIP من GitHub وفك الضغط.

### الخطوة 2: نسخ الملفات
انسخ المجلد إلى:
- **XAMPP:** `C:\xampp\htdocs\alabasi-accounting`
- **WAMP:** `C:\wamp64\www\alabasi-accounting`

### الخطوة 3: إنشاء قاعدة البيانات
1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. أنشئ قاعدة بيانات جديدة: `alabasi_unified`
3. اختر الترميز: `utf8mb4_unicode_ci`

### الخطوة 4: استيراد البيانات
1. اختر قاعدة البيانات `alabasi_unified`
2. اضغط "استيراد" (Import)
3. اختر ملف `database.sql`
4. اضغط "تنفيذ" (Go)

### الخطوة 5: تكوين الاتصال
عدّل ملف `includes/db.php`:
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'alabasi_unified');
define('DB_USER', 'root');
define('DB_PASS', '');  // فارغة في XAMPP
```

### الخطوة 6: افتح النظام
```
http://localhost/alabasi-accounting/
```

**بيانات الدخول:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

---

## 🌐 الطريقة الثالثة: التثبيت التلقائي

إذا كنت على استضافة ويب:

### الخطوة 1: رفع الملفات
ارفع جميع الملفات عبر FTP إلى مجلد `public_html`

### الخطوة 2: افتح معالج التثبيت
```
http://your-domain.com/install.php
```

### الخطوة 3: اتبع التعليمات
- سيفحص النظام المتطلبات تلقائياً
- أدخل بيانات قاعدة البيانات
- اضغط "تثبيت"

### الخطوة 4: احذف ملف التثبيت
بعد التثبيت، احذف ملف `install.php` لأسباب أمنية.

---

## 🔧 حل المشاكل الشائعة

### مشكلة: صفحة بيضاء
**الحل:**
```php
// أضف في أول ملف index.php
ini_set('display_errors', 1);
error_reporting(E_ALL);
```

### مشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل:**
- تحقق من بيانات الاتصال في `includes/db.php`
- تأكد من تشغيل MySQL
- تأكد من صلاحيات المستخدم

### مشكلة: العربية تظهر رموز غريبة
**الحل:**
```sql
ALTER DATABASE alabasi_unified CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### مشكلة: لا يمكن رفع الملفات
**الحل:**
```bash
chmod 755 uploads/
chmod 755 backups/
```

---

## 📚 الخطوات التالية

بعد تشغيل النظام:

1. **غيّر كلمة المرور** من الإعدادات
2. **أضف مستخدمين جدد** من إدارة المستخدمين
3. **راجع التوثيق الكامل** في `README.md`
4. **اقرأ دليل النشر** في `DEPLOYMENT.md`

---

## 🆘 تحتاج مساعدة؟

- 📖 راجع [التوثيق الكامل](README.md)
- 🌐 راجع [دليل النشر](DEPLOYMENT.md)
- 🐛 افتح [Issue على GitHub](https://github.com/alabasi2025/alabasi-accounting-system/issues)

---

## 🎯 ميزات سريعة

بعد تسجيل الدخول، جرّب:

- ✅ **لوحة التحكم** - نظرة شاملة على النظام
- ✅ **دليل الحسابات** - إدارة الحسابات المحاسبية
- ✅ **القيود اليومية** - إدخال القيود
- ✅ **سندات القبض والصرف** - إدارة السندات
- ✅ **التقارير المالية** - تقارير شاملة

---

**مبروك! 🎉 النظام يعمل الآن**

للتطوير المستمر، راجع دليل المطورين في التوثيق الكامل.
