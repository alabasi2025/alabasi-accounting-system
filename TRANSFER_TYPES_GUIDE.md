# 🔄 دليل أنواع الترحيل (Transfer Types Guide)

## 🎯 الهدف
توضيح أنواع الترحيل المختلفة بين الوحدات والمؤسسات لتسهيل العمل مع الذكاء الاصطناعي

---

## 📋 أنواع الترحيل الأربعة

### 1️⃣ **بين الوحدات** (`unit_to_unit`)

**الوصف:** ترحيل سند من وحدة إلى وحدة أخرى

**متى يُستخدم:**
- تحويل أموال بين وحدتين مختلفتين
- شراء من وحدة أخرى
- بيع لوحدة أخرى

**مثال:**
```
الوحدة 1 (المصدر) → الوحدة 2 (الهدف)
السند: دفع 50,000 ريال للوحدة 2
النوع: unit_to_unit
```

**القيود المحاسبية:**

**في الوحدة 1 (المصدر):**
```
من ح/ حساب وسيط - الوحدة 2    50,000 مدين
إلى ح/ الصندوق                 50,000 دائن
```

**في الوحدة 2 (الهدف):**
```
من ح/ الحساب المستهدف          50,000 مدين
إلى ح/ حساب وسيط - الوحدة 1   50,000 دائن
```

---

### 2️⃣ **بين المؤسسات** (`company_to_company`)

**الوصف:** ترحيل سند من مؤسسة إلى مؤسسة أخرى (داخل نفس الوحدة)

**متى يُستخدم:**
- تحويل أموال بين مؤسستين في نفس الوحدة
- مشتريات داخلية بين المؤسسات
- مبيعات داخلية بين المؤسسات

**مثال:**
```
المؤسسة 1 (المصدر) → المؤسسة 2 (الهدف)
السند: تحصيل 75,000 ريال من المؤسسة 2
النوع: company_to_company
```

**القيود المحاسبية:**

**في المؤسسة 1 (المصدر):**
```
من ح/ الصندوق                      75,000 مدين
إلى ح/ حساب وسيط - المؤسسة 2      75,000 دائن
```

**في المؤسسة 2 (الهدف):**
```
من ح/ حساب وسيط - المؤسسة 1       75,000 مدين
إلى ح/ الحساب المستهدف            75,000 دائن
```

---

### 3️⃣ **من وحدة إلى مؤسسة** (`unit_to_company`)

**الوصف:** ترحيل سند من وحدة إلى مؤسسة في وحدة أخرى

**متى يُستخدم:**
- تحويل من الوحدة الرئيسية إلى مؤسسة فرعية
- دعم مالي من الوحدة لمؤسسة معينة
- عمليات بين مستويات تنظيمية مختلفة

**مثال:**
```
الوحدة 1 (المصدر) → المؤسسة 3 في الوحدة 2 (الهدف)
السند: دفع 100,000 ريال للمؤسسة 3
النوع: unit_to_company
```

**القيود المحاسبية:**

**في الوحدة 1 (المصدر):**
```
من ح/ حساب وسيط - المؤسسة 3      100,000 مدين
إلى ح/ الصندوق                    100,000 دائن
```

**في المؤسسة 3 (الهدف):**
```
من ح/ الحساب المستهدف             100,000 مدين
إلى ح/ حساب وسيط - الوحدة 1      100,000 دائن
```

---

### 4️⃣ **من مؤسسة إلى وحدة** (`company_to_unit`)

**الوصف:** ترحيل سند من مؤسسة إلى وحدة

**متى يُستخدم:**
- تحويل من مؤسسة فرعية إلى الوحدة الرئيسية
- تحويل أرباح من المؤسسة للوحدة
- سداد التزامات للوحدة الأم

**مثال:**
```
المؤسسة 2 (المصدر) → الوحدة 1 (الهدف)
السند: تحويل 80,000 ريال للوحدة 1
النوع: company_to_unit
```

**القيود المحاسبية:**

**في المؤسسة 2 (المصدر):**
```
من ح/ حساب وسيط - الوحدة 1       80,000 مدين
إلى ح/ الصندوق                    80,000 دائن
```

**في الوحدة 1 (الهدف):**
```
من ح/ الحساب المستهدف             80,000 مدين
إلى ح/ حساب وسيط - المؤسسة 2     80,000 دائن
```

---

## 🤖 كيف يساعد الذكاء الاصطناعي؟

### أوامر بسيطة للذكاء الاصطناعي:

#### 1. الترحيل بين الوحدات:
```
"رحّل السند SV-2025-001 من الوحدة 1 إلى الوحدة 2"
"أنشئ قيد مقابل للسند بين الوحدات رقم SV-2025-001"
```

#### 2. الترحيل بين المؤسسات:
```
"رحّل السند RV-2025-010 من المؤسسة 1 إلى المؤسسة 2"
"أنشئ قيد مقابل للسند بين المؤسسات رقم RV-2025-010"
```

#### 3. من وحدة إلى مؤسسة:
```
"رحّل السند من الوحدة 1 إلى المؤسسة 3"
```

#### 4. من مؤسسة إلى وحدة:
```
"رحّل السند من المؤسسة 2 إلى الوحدة 1"
```

---

## 📊 جدول مقارنة سريع

| النوع | المصدر | الهدف | الاستخدام الشائع |
|------|--------|-------|------------------|
| `unit_to_unit` | وحدة | وحدة | تحويلات بين وحدات منفصلة |
| `company_to_company` | مؤسسة | مؤسسة | تحويلات داخلية في نفس الوحدة |
| `unit_to_company` | وحدة | مؤسسة | دعم من الوحدة الأم للمؤسسة |
| `company_to_unit` | مؤسسة | وحدة | تحويل أرباح أو سداد للوحدة الأم |

---

## 🔍 كيفية تحديد النوع تلقائياً

عند إنشاء سند معلق، يتم تحديد `transferType` تلقائياً بناءً على:

```php
if ($sourceType == 'unit' && $targetType == 'unit') {
    $transferType = 'unit_to_unit';
} elseif ($sourceType == 'company' && $targetType == 'company') {
    $transferType = 'company_to_company';
} elseif ($sourceType == 'unit' && $targetType == 'company') {
    $transferType = 'unit_to_company';
} elseif ($sourceType == 'company' && $targetType == 'unit') {
    $transferType = 'company_to_unit';
}
```

---

## 📝 استعلامات SQL مفيدة

### 1. عرض السندات المعلقة حسب النوع:

```sql
SELECT 
    transferType,
    COUNT(*) AS count,
    SUM(amount) AS totalAmount
FROM pending_vouchers
WHERE status = 'pending'
GROUP BY transferType;
```

### 2. السندات المعلقة بين الوحدات فقط:

```sql
SELECT * 
FROM pending_vouchers
WHERE transferType = 'unit_to_unit' 
  AND status = 'pending'
ORDER BY priority DESC, voucherDate ASC;
```

### 3. السندات المعلقة بين المؤسسات فقط:

```sql
SELECT * 
FROM pending_vouchers
WHERE transferType = 'company_to_company' 
  AND status = 'pending'
ORDER BY priority DESC, voucherDate ASC;
```

### 4. السندات المعلقة من وحدات إلى مؤسسات:

```sql
SELECT * 
FROM pending_vouchers
WHERE transferType = 'unit_to_company' 
  AND status = 'pending'
ORDER BY priority DESC, voucherDate ASC;
```

---

## 🎯 فوائد تصنيف أنواع الترحيل

### 1. **وضوح أكبر:**
- يعرف المستخدم فوراً نوع العملية
- سهولة الفهم والتتبع

### 2. **تسهيل عمل الذكاء الاصطناعي:**
- أوامر بسيطة وواضحة
- تقليل الأخطاء في الترحيل
- فهم أفضل للسياق

### 3. **تقارير أفضل:**
- إحصائيات حسب نوع الترحيل
- تحليل أنماط التحويلات
- رصد الحركة بين الكيانات

### 4. **صلاحيات محددة:**
- يمكن تحديد صلاحيات حسب نوع الترحيل
- مثلاً: مستخدم يمكنه الترحيل بين المؤسسات فقط

---

## 💡 أمثلة عملية

### مثال 1: شركة متعددة الفروع

```
الوحدة 1: المقر الرئيسي
  ├─ المؤسسة 1: فرع الرياض
  ├─ المؤسسة 2: فرع جدة
  └─ المؤسسة 3: فرع الدمام

الوحدة 2: المصنع
  └─ المؤسسة 4: مصنع الإنتاج
```

**السيناريوهات:**

1. **تحويل من المقر إلى المصنع:**
   - النوع: `unit_to_unit`
   - من: الوحدة 1 → إلى: الوحدة 2

2. **تحويل من فرع الرياض إلى فرع جدة:**
   - النوع: `company_to_company`
   - من: المؤسسة 1 → إلى: المؤسسة 2

3. **دعم من المقر لفرع الدمام:**
   - النوع: `unit_to_company`
   - من: الوحدة 1 → إلى: المؤسسة 3

4. **تحويل أرباح من المصنع للمقر:**
   - النوع: `company_to_unit`
   - من: المؤسسة 4 → إلى: الوحدة 1

---

## ✅ الخلاصة

| العنصر | التفاصيل |
|--------|----------|
| **عدد الأنواع** | 4 أنواع رئيسية |
| **التحديد** | تلقائي بناءً على المصدر والهدف |
| **الفائدة** | وضوح + سهولة + تقارير أفضل |
| **الذكاء الاصطناعي** | أوامر بسيطة ومباشرة |

---

**تاريخ الإنشاء:** 2025-01-14  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهز للاستخدام
