# 📥 تعليمات تحديث السيرفر المحلي

## 🎯 الهدف
تحديث السيرفر المحلي (XAMPP) من GitHub للحصول على آخر التحديثات

---

## 🚀 الطريقة الأولى: التحديث الكامل (موصى به)

### الخطوات:

1. **افتح PowerShell كمسؤول (Run as Administrator)**
   - اضغط `Win + X`
   - اختر "Windows PowerShell (Admin)"

2. **انتقل إلى مجلد المشروع:**
   ```powershell
   cd D:\AAAAAA\alabasi-xampp-system
   ```

3. **شغّل سكريبت التحديث الكامل:**
   ```powershell
   .\update-local-server.ps1
   ```

### ✨ المميزات:
- ✅ يعرض ملخص التحديثات
- ✅ يحفظ نسخة احتياطية من التغييرات المحلية
- ✅ يعرض الملفات المتغيرة
- ✅ يعرض إحصائيات التحديث
- ✅ خيارات إضافية (فتح المجلد، phpMyAdmin، إلخ)

---

## ⚡ الطريقة الثانية: التحديث السريع

### الخطوات:

1. **افتح PowerShell**

2. **انتقل إلى مجلد المشروع:**
   ```powershell
   cd D:\AAAAAA\alabasi-xampp-system
   ```

3. **شغّل سكريبت التحديث السريع:**
   ```powershell
   .\quick-update.ps1
   ```

### ✨ المميزات:
- ⚡ سريع جداً
- ✅ يسحب التحديثات فوراً
- ✅ يعرض آخر commit

---

## 🔧 الطريقة الثالثة: أوامر Git يدوية

### الخطوات:

```powershell
# 1. الانتقال إلى المجلد
cd D:\AAAAAA\alabasi-xampp-system

# 2. التحقق من الحالة
git status

# 3. حفظ التغييرات المحلية (إذا لزم الأمر)
git add .
git commit -m "Backup: نسخة احتياطية"

# 4. سحب التحديثات
git pull origin master

# 5. عرض آخر التحديثات
git log -5 --oneline
```

---

## ⚠️ حل المشاكل الشائعة

### مشكلة 1: "execution of scripts is disabled"

**الحل:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

ثم حاول مرة أخرى.

---

### مشكلة 2: "Your local changes would be overwritten"

**الحل:**
```powershell
# حفظ التغييرات المحلية
git stash

# سحب التحديثات
git pull origin master

# استرجاع التغييرات المحلية
git stash pop
```

---

### مشكلة 3: "fatal: not a git repository"

**الحل:**
المجلد ليس مرتبطاً بـ Git. قم بالاستنساخ من جديد:

```powershell
cd C:\xampp\htdocs
git clone https://github.com/alabasi2025/alabasi-accounting-system.git alabasi-xampp-system
```

---

## 📋 بعد التحديث

### 1. تنفيذ ملفات SQL الجديدة (إذا وجدت)

**الملفات المهمة:**
- `sql/pending_vouchers_table.sql`
- `sql/intermediate_account_transactions.sql`

**الخطوات:**
1. افتح phpMyAdmin: http://localhost/phpmyadmin
2. اختر قاعدة البيانات: `alabasi_unified`
3. اذهب إلى تبويب "SQL"
4. افتح الملف ونسخ محتواه
5. الصق المحتوى وانقر "Go"

---

### 2. التحقق من الملفات الجديدة

**افتح المجلد وتحقق من:**
- ✅ الملفات الجديدة في `sql/`
- ✅ التوثيق الجديد (`.md` files)
- ✅ الواجهات الجديدة (`.php` files)
- ✅ ملفات JavaScript الجديدة (`js/`)

---

### 3. اختبار النظام

1. **افتح المتصفح:**
   ```
   http://localhost/alabasi-xampp-system
   ```

2. **تحقق من:**
   - ✅ الصفحة الرئيسية تعمل
   - ✅ تسجيل الدخول يعمل
   - ✅ الصفحات الجديدة تظهر في القائمة
   - ✅ لا توجد أخطاء PHP

---

## 🔄 جدول التحديثات

| التاريخ | الإصدار | الوصف |
|---------|---------|-------|
| 2025-01-14 | v2.1 | نظام السندات المعلقة + الحساب الوسيط الذكي |
| 2025-01-13 | v2.0 | النظام الموحد - 57 جدول |
| 2025-01-12 | v1.5 | وحدة المخزون الكاملة |

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من سجل الأخطاء (error log)
2. راجع التوثيق في `README.md`
3. تحقق من GitHub Issues

---

## ✅ Checklist بعد التحديث

- [ ] تم سحب التحديثات من GitHub
- [ ] تم تنفيذ ملفات SQL الجديدة
- [ ] تم اختبار الصفحة الرئيسية
- [ ] تم اختبار تسجيل الدخول
- [ ] تم التحقق من الصفحات الجديدة
- [ ] لا توجد أخطاء PHP
- [ ] تم مراجعة التوثيق الجديد

---

**تاريخ آخر تحديث:** 2025-01-14  
**الإصدار:** 2.1  
**الحالة:** ✅ جاهز للاستخدام
